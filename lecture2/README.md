# API request

To run the code, use the following command: `poetry run uvicorn main:app --reload`.
Then go to http://127.0.0.1:8000/

The "1" is shown in the main url, no need of adding anything to the path.